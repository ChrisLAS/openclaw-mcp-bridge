{
  "name": "openclaw-mcp-bridge",
  "version": "0.2.0",
  "description": "OpenClaw plugin that bridges MCP HTTP servers as native agent tools",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
